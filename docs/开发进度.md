# FocusGuard 开发进度

## 2026-06-16

- 读取已安装 skills，确认本机 skill 位置。
- 用正确 UTF-8 编码读取 `docs/FocusGuard项目方案.md`。
- 读取相关旧线程，整理用户背景和项目目标。
- 检查当前 Android 项目文件：Gradle 版本目录、根构建文件、app 构建文件、Manifest、MainActivity、主题。
- 创建初始计划、发现、进度文档。
- 添加 Navigation Compose、Hilt、Room、DataStore、WorkManager、Lifecycle Compose、Material Icons、KSP 等依赖。
- 添加分层 Kotlin 骨架：domain 模型/分析器、Room Entity/DAO/Database、Repository 接口和演示实现、Hilt Module、Service 和 Worker 占位。
- 将默认 MainActivity 替换为 FocusGuard Compose App 壳。
- 添加底部导航和 Today、Session、Reports、Settings、SessionDetail、PermissionGuide、Onboarding 页面。
- 将主题从默认紫色调整为更适合工具型 App 的青绿/冷灰方向。
- 修复缺少 `android.useAndroidX=true` 的构建问题。
- 修复 Kotlin 与 Java JVM target 不一致问题。
- 将 Kotlin target 设置从废弃的 `kotlinOptions` 迁移到 `compilerOptions`。
- 最终验证通过：`.\gradlew.bat :app:assembleDebug`。
- 当时 `git status --short` 无法运行，因为目录还不是 Git 仓库。
- 添加 `docs/AI实施规范.md`，规定后续 AI 如何读取上下文、实现功能、保护学习目标、验证构建和沉淀文档。

## 2026-06-17

- 实现第一版学习用专注计时状态机，暂时只保存在内存中。
- 更新 `SessionUiState`，加入 `Paused` 状态，使专注页具备明确的 `Ready -> Running -> Paused -> Finished` 状态。
- 重写 `SessionViewModel`，让开始、暂停、继续、结束、重置事件都在 ViewModel 中处理，而不是放在 Composable 中。
- 更新 `SessionScreen`，按钮只调用 ViewModel 事件回调，并根据状态展示运行、暂停、完成界面。
- 验证通过：`.\gradlew.bat :app:assembleDebug`。

## 2026-06-21

- 给关键架构和数据流文件添加中文学习注释。
- 注释覆盖入口、导航壳、路由定义、UiState 状态机、SessionViewModel 计时逻辑、DAO/Room 边界、Repository 合约、演示 Repository、Analyzer 职责、Hilt Module 绑定。
- 注释重点解释“为什么这样做”和“层与层如何协作”，避免逐行机械注释。
- 验证通过：`.\gradlew.bat :app:assembleDebug`。
- 根据用户要求整理文档结构：项目级长期规则放根目录，开发过程资料放 `docs`。
- 新增 `Git提交规范.md`，规定小步提交、提交信息格式和 AI 提醒规则。
- 将英文内容的计划、发现、进度文档改为中文内容和中文文件名。
- 开始阶段 2 的 Room 持久化闭环：新增 `FocusSessionMapper`，用单元测试验证 `FocusSessionEntity` 与 `FocusSession` 的互转。
- `FocusSessionDao` 增加按 id 查询和结束会话更新能力。
- `FocusRepositoryImpl` 从内存演示列表切换为 Room Flow 查询，`startSession` 会插入进行中记录，`finishSession` 会更新同一条记录。
- `SessionViewModel` 开始通过 `FocusRepository` 创建和结束真实会话，并把暂停后的净专注时长传给数据层保存。
- 已验证 mapper 单元测试：`.\gradlew.bat :app:testDebugUnitTest --tests "com.ywb.focusguard.data.local.mapper.FocusSessionMapperTest"` 通过。
- 修复 Today 到 Session 的跳转逻辑：抽出统一的顶层导航函数，让首页按钮和底部 Tab 使用同一套 `popUpTo / launchSingleTop / restoreState` 规则。
- 将专注详情路由从 `session/{sessionId}` 改为 `session_detail/{sessionId}`，避免与专注页 `session` route 混淆。
- 新增 `DestinationTest` 覆盖详情路由生成规则。
- 已验证导航测试：`.\gradlew.bat :app:testDebugUnitTest --tests "com.ywb.focusguard.ui.navigation.DestinationTest"` 通过。
- 已验证构建：`.\gradlew.bat :app:assembleDebug` 通过。
- 完成 SessionDetail 真实数据接入：新增 `SessionDetailViewModel`，通过 `SavedStateHandle` 读取 `sessionId`，再订阅 `FocusRepository.observeSessionDetail(sessionId)`。
- 新增 `SessionDetailUiState` 和 `toUiState()`，把领域层详情转换为页面可直接展示的标题、时长、评分、评分拆解和建议文案。
- `SessionDetailScreen` 从硬编码内容改为由 `SessionDetailUiState` 驱动，并补充 Loading / Empty / Content 三种展示状态。
- 新增 `SessionDetailUiStateTest` 覆盖详情 UI 状态映射。
- 进入阶段 3，完成光照传感器第一版接入。
- 新增 `LightLevelClassifier`，将 lux 映射为 `DARK / DIM / COMFORTABLE / BRIGHT`。
- 新增 `LightSensorDataSource`，使用 `SensorManager.TYPE_LIGHT` 注册光照监听，并通过 `callbackFlow` 暴露 `Flow<LightSample>`。
- `LightSensorDataSource` 在 `awaitClose` 中注销监听，避免页面停止收集后传感器仍然占用资源。
- `EnvironmentRepositoryImpl.observeLight()` 已从固定演示数据切换为真实光照数据源，噪声和移动仍保持演示数据。
- 已验证光照规则测试：`.\gradlew.bat :app:testDebugUnitTest --tests "com.ywb.focusguard.data.sensor.LightLevelClassifierTest"` 通过。
- 完成移动传感器第一版接入。
- 新增 `MotionClassifier`，使用 `sqrt(x*x + y*y + z*z)` 计算加速度模长，并用与 9.8 的偏差判断是否明显移动。
- 新增 `MotionSensorDataSource`，使用 `SensorManager.TYPE_ACCELEROMETER` 注册加速度计监听，并通过 `callbackFlow` 暴露 `Flow<MotionSample>`。
- `MotionSensorDataSource` 在 `awaitClose` 中注销监听，避免页面停止收集后加速度计仍然占用资源。
- `EnvironmentRepositoryImpl.observeMotion()` 已从固定演示数据切换为真实移动数据源，噪声仍保持演示数据。
- 已验证移动规则测试：`.\gradlew.bat :app:testDebugUnitTest --tests "com.ywb.focusguard.data.sensor.MotionClassifierTest"` 通过。

## 2026-07-16

- 新增 `MotionEventDetector`，将高频加速度计样本聚合为离散的有效移动事件。
- 第一版规则为：600ms 内连续 3 次超过阈值后确认一次移动，随后进入 1200ms 冷却期。
- `MotionSensorDataSource` 使用 `event.timestamp` 的单调时间执行窗口和冷却判断，使用 `System.currentTimeMillis()` 生成后续可存入 Room 的现实时间戳。
- `MotionSample.isSignificantMove` 现在表示“刚确认了一次新移动事件”，不再表示单帧加速度是否越过阈值。
- 新增 `MotionEventDetectorTest`，覆盖单次命中、连续命中、窗口超时、冷却期、冷却恢复和稳定样本中断等场景。
- 更新 `AGENTS.md` 和 `AI实施规范.md`：后续 AI 生成或修改代码时，默认补充中文学习注释，并重点覆盖类、关键属性、公开方法、状态字段、时间/阈值和资源生命周期。
- 对当前生产代码和单元测试完成系统注释整理，覆盖 Room Entity/DAO、领域模型、Repository、传感器、Hilt、Service 占位、UiState、ViewModel、Navigation、Screen、通用组件和主题。
- 修正过期注释与页面说明，明确当前光照/移动为真实传感器数据，噪声、采样曲线、DataStore、前台服务仍处于演示或占位阶段。
- 使用本机 `E:\jdk21` 验证：`.\gradlew.bat :app:testDebugUnitTest` 通过，`.\gradlew.bat :app:assembleDebug` 通过。
- 记录 Gradle 环境：默认 `JAVA_HOME=E:\jdk17` 会触发 JDK 21 工具链下载；本机已有 `E:\jdk21`，后续验证应优先临时使用该路径。

## 2026-08-19

- 完成 Task 7：将 SettingsRepository 从内存实现替换为 DataStore 持久化实现。
- 新增 `DataStoreSettingsRepository`，使用 Jetpack DataStore Preferences 持久化用户设置。
- 使用 `preferencesDataStore` 委托创建 DataStore 实例，自动处理文件锁和并发访问。
- Key 设计：每个设置项使用独立的 Preferences Key，与 UserSettings 字段一一对应。
- 修改 `AppModule.kt`，将 SettingsRepository 绑定从 `SettingsRepositoryImpl`（内存）改为 `DataStoreSettingsRepository`（DataStore）。
- 新增 `DataStoreSettingsRepositoryTest` 单元测试，验证默认值、各 update 方法持久化、Flow 响应式更新。
- 修复 `MotionClassifierTest` 和 `MotionEventDetectorTest` 编译错误，使其匹配当前 API 签名。
- 添加 `kotlinx-coroutines-test` 和 `robolectric` 测试依赖。
- 验证通过：单元测试 6/6 通过，`.\gradlew.bat :app:assembleDebug` 构建成功。

## 2026-08-20

- 完成阶段 6 报告优化：
  - DAO 增加按日期范围查询方法
  - ReportsViewModel 支持周/月切换，使用 flatMapLatest 实现周期切换时自动重查 Room
  - ReportsScreen 移除 demo 数据，接入真实聚合统计
  - 新增 PeriodSummary 聚合统计模型
- DataStore 替换内存设置：
  - 新增 DataStoreSettingsRepository，使用 Preferences DataStore 持久化用户设置
  - Hilt 绑定切换为 DataStore 实现
  - 设置页支持点击编辑（默认时长、噪声阈值循环切换预设值）
- 清理过时文案：TodayScreen、RepositoryContracts、SessionViewModel 中的过时描述
- 验证通过：构建和单元测试均通过

## 2026-09-11

- 选择“理性数据”视觉方向，建立青绿主色、暖白背景、深墨文字和蓝色选中态组成的 FocusGuard 1.0 设计系统。
- 重构 Today：环境评分成为第一视觉层级，统一噪声/光照/手机状态行，保留唯一主操作“开始专注”，修复专注次数错误使用分心次数的数据映射问题。
- 重构 Session：加入 25/45/60 分钟选择、开始前环境预检、沉浸式运行计时、暂停和完成总结；修复暂停期间仍累计有效时长的问题。
- 重构 Reports、SessionDetail 和 Settings：报告接入真实 Room 聚合数据，详情展示环境图表与评分拆解，设置开关接入 DataStore 持久化。
- 加入三步首次引导，并用 DataStore 保存完成状态；底部导航精简为 Today / Reports / Settings，专注会话改为沉浸式临时页面。
- 补齐每日总结闭环：设置开关持久化后创建/取消唯一 WorkManager 周期任务，每晚 21:00 读取当天 Room 记录并发送本地汇总通知。
- 环境检测空值统一显示“检测中”，避免传感器尚未返回数据时错误展示“稳定”。
- 接入 Compose Preview Screenshot Testing，为 Today、Session Ready、Session Running、Reports、Settings 建立 390 × 844 dp 截图回归基线。
- 完成设计稿与 Today 实现的并排视觉 QA；刻意未复制无真实历史支撑的迷你趋势线，避免把装饰性数据误认为传感器记录。
- 最终验证：25 个单元测试全部通过，5 个截图回归通过，`:app:assembleDebug` 构建成功。
- 修复专注准备页和运行页持续闪烁：`AnimatedContent` 改用稳定的页面阶段作为 `contentKey`，传感器采样与每秒计时只触发普通重组。
- 新增 `SessionVisualPhaseTest` 覆盖准备页数据更新、运行计时更新和真正状态切换的动画 key 行为。
- 全面测试时额外发现并修复 Android 12L 及以下通知权限误判、前台服务类型兼容、默认 Locale 和遗留无用颜色资源问题。
